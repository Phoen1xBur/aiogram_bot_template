# Template для Telegram бота на aiogram 3.x

Это шаблон структуры для создания Telegram ботов на Python с использованием библиотеки **aiogram 3.x**. Проект организован с четким разделением ответственности и модульной архитектурой.
<details>
<summary>📋 Содержание (Click to show)</summary>

- [Особенности](#особенности)
- [Структура проекта](#структура-проекта)
- [Установка](#установка)
- [Настройка](#настройка)
- [Запуск](#запуск)
- [Архитектура](#архитектура)
- [Примеры использования](#примеры-использования)
</details>

## ✨Особенности

- ✅ Модульная структура с разделением обработчиков по типам чатов
- ✅ Поддержка middleware для расширения функциональности
- ✅ Кастомные фильтры для удобной обработки сообщений
- ✅ Готовые примеры inline и reply клавиатур
- ✅ Конфигурация через `.env` файл с использованием `pydantic-settings`
- ✅ Чистая архитектура с разделением на слои

## 📁Структура проекта

```
bot/
├── run.py                 # Точка входа для запуска бота
├── config.py              # Конфигурация и настройки
├── handlers/              # Обработчики сообщений
│   ├── __init__.py        # Экспорт всех роутеров
│   ├── private/           # Обработчики для приватных чатов
│   │   ├── __init__.py
│   │   └── common.py      # Примеры обработчиков
│   ├── group/             # Обработчики для групповых чатов
│   │   ├── __init__.py
│   │   └── common.py
│   └── channel/           # Обработчики для каналов
│       ├── __init__.py
│       └── common.py
├── middleware/            # Middleware для обработки обновлений
│   ├── __init__.py        # Экспорт middleware
│   └── redis_context.py   # Пример middleware для Redis
├── keyboards/             # Клавиатуры
│   ├── __init__.py
│   ├── inline_test.py     # Пример inline клавиатуры
│   └── reply_test.py      # Пример reply клавиатуры
└── utils/                 # Вспомогательные утилиты
    └── filters.py         # Кастомные фильтры
.env.default               # Пример переменных окружения
requirements.txt           # Зависимости проекта
```

## 🚀Установка

1. **Клонируйте репозиторий или используйте как шаблон**
    ```bash
   git clone https://github.com/Phoen1xBur/aiogram_bot_template.git
   ```
2. **Создайте и активируйте виртуальное окружение**
   ```bash
   cd aiogram_bot_template
   python3 -m venv .venv
   source .venv/bin/activate
   ```

2. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

## ⚙️ Настройка <a name="настройка"></a>

1. **Создайте файл `.env` в корне проекта, или скопируйте/переименуйте существующий `.env.default`:**
   ```env
   TOKEN=your_bot_token_here
   ```

2. **Получите токен бота:**
   - Откройте [@BotFather](https://t.me/BotFather) в Telegram
   - Создайте нового бота командой `/newbot`
   - Скопируйте полученный токен в файл `.env`

## ▶️Запуск <a name="запуск"></a>

Запустите бота командой:

```bash
python bot/run.py
```

## 🏗️ Архитектура <a name="архитектура"></a>

### Обработчики (Handlers)

Обработчики организованы по типам чатов для удобства управления:

- **`handlers/private/`** - обработчики для приватных чатов с ботом
- **`handlers/group/`** - обработчики для групповых чатов
- **`handlers/channel/`** - обработчики для каналов

Каждый модуль содержит роутеры, которые автоматически фильтруют сообщения по типу чата.

### Middleware

Middleware позволяют перехватывать и обрабатывать обновления до того, как они достигнут обработчиков. Пример использования:

```python
# bot/middleware/redis_context.py
class AddRedisContext(BaseMiddleware):
    async def __call__(self, handler, event, data):
        # Добавьте сюда логику работы с Redis
        # data["redis"] = redis
        return await handler(event, data)
```

### Кастомные фильтры

В проекте реализованы полезные фильтры в `bot/utils/filters.py`:

- **`ChatTypeFilter`** - фильтрация по типу чата (PRIVATE, GROUP, CHANNEL)
- **`MessageTypeFilter`** - фильтрация по типу контента сообщения
- **`WordFilter`** - проверка наличия целых слов в тексте сообщения

Пример использования: {#примеры-использования}

```python
from utils.filters import ChatTypeFilter, WordFilter
from aiogram.enums import ChatType

router.message.filter(ChatTypeFilter(ChatType.PRIVATE))

@router.message(WordFilter(words=('Вася', 'Василий'), ignore_case=True))
async def handler(message: Message):
    await message.reply('Привет!')
```

### Клавиатуры

Примеры создания клавиатур находятся в `bot/keyboards/`:

- **`inline_test.py`** - пример inline клавиатуры
- **`reply_test.py`** - пример reply клавиатуры

## 📝 Примеры использования <a name="примеры-использования"></a>

### Добавление нового обработчика

1. Создайте новый файл в соответствующей директории (например, `bot/handlers/private/admin.py`):

```python
from aiogram import Router, F
from aiogram.types import Message
from utils.filters import ChatTypeFilter
from aiogram.enums import ChatType

router = Router(name=__name__)
router.message.filter(ChatTypeFilter(ChatType.PRIVATE))

@router.message(F.text == '/admin')
async def admin_command(message: Message):
    await message.reply('Админ панель')
```

2. Добавьте роутер в `bot/handlers/private/__init__.py`:

```python
from . import common
from . import admin

routers = (
    common.router,
    admin.router,  # Добавьте новый роутер
)
```

### Добавление нового middleware

1. Создайте файл в `bot/middleware/` (например, `logging.py`)
2. Добавьте его в `bot/middleware/__init__.py`:

```python
from . import redis_context
from . import logging

middlewares = [
    redis_context.AddRedisContext(),
    logging.LoggingMiddleware(),  # Добавьте новый middleware
]
```

### Использование конфигурации

Расширьте `bot/config.py` для добавления новых настроек:

```python
from pydantic_settings import BaseSettings, SettingsConfigDict

class Settings(BaseSettings):
    TOKEN: str
    ADMIN_ID: int = 123456789
    DATABASE_URL: str = "sqlite:///bot.db"

    model_config = SettingsConfigDict(env_file='.env')

settings = Settings()
```

## 📦Зависимости

Основные зависимости проекта:

- **aiogram** (3.22.0) - современная библиотека для работы с Telegram Bot API
- **pydantic-settings** (2.11.0) - управление настройками через переменные окружения
- **python-dotenv** (1.2.1) - загрузка переменных из `.env` файла

Полный список зависимостей смотрите в `requirements.txt`.

## 🔧Дальнейшая разработка

Рекомендации для расширения проекта:

1. **База данных**: Добавьте интеграцию с Redis, PostgreSQL или MongoDB
2. **Логирование**: Настройте структурированное логирование
3. **Обработка ошибок**: Добавьте глобальные обработчики исключений
4. **Админ-панель**: Реализуйте систему прав доступа
5. **Тестирование**: Добавьте unit и integration тесты

## 📄Лицензия

Этот шаблон предоставляется как есть для использования в ваших проектах.

## 🤝Вклад

Если вы хотите улучшить этот шаблон, создайте issue или pull request!

---

**Приятной разработки! 🚀**

